<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:bv="clr-namespace:RoSatGCS.Behaviors"
                    xmlns:cvt="clr-namespace:RoSatGCS.Utils.Converter"
                    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                    xmlns:ap="clr-namespace:RoSatGCS.Utils.AttachedProperties"
                    xmlns:loc="clr-namespace:RoSatGCS.Utils.Localization"
                    xmlns:local="clr-namespace:RoSatGCS.Views"
                    xmlns:ctr="clr-namespace:RoSatGCS.Controls"
                    xmlns:vm="clr-namespace:RoSatGCS.ViewModels">
    <DataTemplate x:Key="PaneTemplatePassQueue" DataType="{x:Type vm:PanePassQueueViewModel}">
        <Grid Margin="10,5,10,5">
            <Grid.Resources>
                <Style x:Key="Splitter" TargetType="Rectangle">
                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                    <Setter Property="Width" Value="1"/>
                    <Setter Property="Margin" Value="3,9,3,9"/>
                    <Setter Property="Stroke" Value="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    <Button Style="{DynamicResource PaneButton}" ToolTip="{loc:Localization sExport}">
                        <Rectangle Style="{DynamicResource Icon}" Height="14" Width="12">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource IconExport}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button>
                    <Button Style="{DynamicResource PaneButton}" ToolTip="{loc:Localization sImport}">
                        <Rectangle Style="{DynamicResource Icon}" Height="14" Width="12">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource IconImport}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button>
                    
                    <Rectangle Style="{StaticResource Splitter}"/>

                    <Button Style="{DynamicResource PaneButton}" ToolTip="{loc:Localization sRun}">
                        <Rectangle Style="{DynamicResource Icon}" Height="12" Width="10">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource IconRun}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button>

                    <Rectangle Style="{StaticResource Splitter}"/>
                    
                    <Button Style="{DynamicResource PaneButton}" ToolTip="{loc:Localization sDeleteAll}">
                        <Rectangle Style="{DynamicResource Icon}" Height="10" Width="10">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource IconTrash}"/>
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button>
                </StackPanel>

                <!-- ="{Binding SearchString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" -->
                <TextBox MinWidth="30" Margin="0,0,24,0" Grid.Column="1" Height="24" adonisExtensions:WatermarkExtension.Watermark="{loc:Localization sSearch}"/>
                <!-- Command="{Binding SearchClear}" -->
                <Button Grid.Column="1" HorizontalAlignment="Right" Style="{DynamicResource PaneButton}" ToolTip="{loc:Localization sClear}" >
                    <Rectangle Style="{DynamicResource Icon}">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource IconClear}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Button>
            </Grid>

            <ListView Grid.Row="1">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" Width="30">
                            
                        </GridViewColumn>

                        <GridViewColumn Width="20">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Name" Width="100">
                            
                        </GridViewColumn>

                        <GridViewColumn Header="Date" Width="70">

                        </GridViewColumn>

                        <GridViewColumn Header="AOS" Width="60">

                        </GridViewColumn>

                        <GridViewColumn Header="LOS" Width="60">

                        </GridViewColumn>

                        <GridViewColumn Header="Max. El." Width="70">

                        </GridViewColumn>
                        
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DataTemplate>
</ResourceDictionary>