<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:bv="clr-namespace:RoSatGCS.Behaviors"
                    xmlns:cvt="clr-namespace:RoSatGCS.Utils.Converter"
                    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                    xmlns:ap="clr-namespace:RoSatGCS.Utils.AttachedProperties"
                    xmlns:loc="clr-namespace:RoSatGCS.Utils.Localization"
                    xmlns:local="clr-namespace:RoSatGCS.Views"
                    xmlns:ctr="clr-namespace:RoSatGCS.Controls"
                    xmlns:vm="clr-namespace:RoSatGCS.ViewModels">

    <DataTemplate x:Key="PaneTemplateTleList" DataType="{x:Type vm:PaneTleListViewModel}">
        <Grid Margin="0,3,0,0">
            <Grid.Resources>
                <Style TargetType="GroupBox">
                    <Setter Property="Margin" Value="10,0,10,5"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="FontSize" Value="10"/>
                    <Setter Property="FontWeight" Value="Normal"/>
                </Style>
                <Style x:Key="Splitter" TargetType="Rectangle">
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="Width" Value="1"/>
                    <Setter Property="Stroke" Value="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="230"/>
            </Grid.RowDefinitions>
            <Border Grid.RowSpan="2" BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}" BorderThickness="0.5" Background="{DynamicResource {x:Static adonisUi:Brushes.Layer0BackgroundBrush}}"/>
            <Label Grid.Row="2" Content="Prediction" VerticalContentAlignment="Bottom" Margin="2,0,0,5" FontWeight="Medium"/>
            <Rectangle Height="1" Grid.Row="2" Stroke="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}" VerticalAlignment="Bottom" Margin="2,0,2,4" HorizontalAlignment="Stretch"/>
            <Border Grid.Row="3" BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer3BorderBrush}}" BorderThickness="0.5" Background="{DynamicResource {x:Static adonisUi:Brushes.Layer0BackgroundBrush}}">
                <Grid Margin="3">
                    <TextBlock Text="Start" Margin="10,9,0,0"/>
                    <Rectangle Style="{StaticResource Splitter}" Height="15" Margin="50,10,0,0"/>
                    <ctr:DateTimePicker Height="26" Width="140" Margin="0,5,10,0" HorizontalAlignment="Right" VerticalAlignment="Top"/>

                    <TextBlock Text="End" Margin="10,39,0,0"/>
                    <Rectangle Style="{StaticResource Splitter}" Height="15" Margin="50,40,0,0"/>
                    <ctr:DateTimePicker Height="26" Width="140" Margin="0,35,10,0" HorizontalAlignment="Right" VerticalAlignment="Top"/>

                    <TextBlock Text="Lat." Margin="10,69,0,0"/>
                    <Rectangle Style="{StaticResource Splitter}" Height="15" Margin="50,70,0,0"/>
                    <TextBlock Text="0.0000" TextAlignment="Center" Height="26" Width="100" Margin="0,69,50,0" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    <TextBlock Text="Deg" Margin="0,69,20,0" HorizontalAlignment="Right"/>

                    <TextBlock Text="Lon." Margin="10,99,0,0"/>
                    <Rectangle Style="{StaticResource Splitter}" Height="15" Margin="50,100,0,0"/>
                    <TextBlock Text="0.0000" TextAlignment="Center" Height="26" Width="100" Margin="0,99,50,0" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    <TextBlock Text="Deg" Margin="0,99,20,0" HorizontalAlignment="Right"/>

                    <TextBlock Text="Alt." Margin="10,129,0,0"/>
                    <Rectangle Style="{StaticResource Splitter}" Height="15" Margin="50,130,0,0"/>
                    <TextBlock Text="0.0" TextAlignment="Center" Height="26" Width="100" Margin="0,129,50,0" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    <TextBlock Text="m" Margin="0,129,25,0" HorizontalAlignment="Right"/>

                    <CheckBox Content="Auto Predict"  HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="12,0,0,35"/>
                    <Button Content="Predict" Width="70" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,32"/>

                    <Rectangle Height="1" Stroke="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}" VerticalAlignment="Bottom" Margin="10,0,10,20" HorizontalAlignment="Stretch"/>
                    <TextBlock Text="Last Prediction: 2 hours ago" FontSize="9" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,3"/>

                </Grid>
            </Border>

            <StackPanel Margin="3" Orientation="Horizontal" Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}" Grid.Row="0">
                <Button Style="{DynamicResource ToolButton}" Margin="3,0,0,0">
                    <Rectangle Style="{DynamicResource Icon}">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource IconAdd}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Button>

                <Rectangle Style="{StaticResource Splitter}" Height="12" Margin="3,0,3,0" VerticalAlignment="Center"/>
                    
                <Button Style="{DynamicResource ToolButton}" Margin="0,0,0,0">
                    <Rectangle Style="{DynamicResource Icon}" Height="10" Width="9">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource IconFileAdd}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Button>
                
                <Button Style="{DynamicResource ToolButton}" Margin="0,0,0,0">
                    <Rectangle Style="{DynamicResource Icon}" Height="10" Width="11">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource IconWeb}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Button>

                <Rectangle Style="{StaticResource Splitter}" Height="12" Margin="3,0,3,0" VerticalAlignment="Center"/>

                <Button Style="{DynamicResource ToolButton}" Margin="0,0,0,0">
                    <Rectangle Style="{DynamicResource Icon}" Height="10" Width="11">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource IconSync}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </Button>

                <TextBlock Text="Updated: 2025-01-01" FontSize="9" Margin="4,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>   
        </Grid>
    </DataTemplate>
</ResourceDictionary>