<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:bv="clr-namespace:RoSatGCS.Behaviors"
                    xmlns:cvt="clr-namespace:RoSatGCS.Utils.Converter"
                    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
                    xmlns:ap="clr-namespace:RoSatGCS.Utils.AttachedProperties"
                    xmlns:loc="clr-namespace:RoSatGCS.Utils.Localization"
                    xmlns:local="clr-namespace:RoSatGCS.Views"
                    xmlns:ctr="clr-namespace:RoSatGCS.Controls"
                    xmlns:vm="clr-namespace:RoSatGCS.ViewModels">

    <DataTemplate x:Key="PaneTemplatePassSchedule" DataType="{x:Type vm:PanePassScheduleViewModel}">
        <Grid Margin="10,5,10,5">
            <Grid.Resources>
                <Style TargetType="GroupBox">
                    <Setter Property="Margin" Value="0,0,0,5"/>
                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="FontWeight" Value="Normal"/>
                </Style>
                <Style x:Key="Splitter" TargetType="Rectangle">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="Width" Value="1"/>
                    <Setter Property="Height" Value="14"/>
                    <Setter Property="Margin" Value="0,3,0,0"/>
                    <Setter Property="Stroke" Value="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="150"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <GroupBox>
                <GroupBox.Header>
                    <TextBlock FontWeight="Bold">Details</TextBlock>
                </GroupBox.Header>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="54"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="54"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Column="0" Grid.Row="0" Text="Name" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="1" Grid.Row="0" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="1" Grid.Row="0" Text="Satellite" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"/>

                    <TextBlock Grid.Column="2" Grid.Row="0" Text="Date" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="3" Grid.Row="0" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="3" Grid.Row="0" Text="2025-01-01" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"/>

                    <TextBlock Grid.Column="0" Grid.Row="1" Text="Max. El." VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="1" Grid.Row="1" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="1" Grid.Row="1" Text="20 deg" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"/>
                    
                    <TextBlock Grid.Column="2" Grid.Row="1" Text="Duration" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="3" Grid.Row="1" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="3" Grid.Row="1" Text="1:00:00" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"/>

                    <TextBlock Grid.Column="0" Grid.Row="2" Text="AOS" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="1" Grid.Row="2" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="1" Grid.Row="2" Text="12:00:00" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"/>

                    <TextBlock Grid.Column="2" Grid.Row="2" Text="LOS" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="3" Grid.Row="2" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="3" Grid.Row="2" Text="13:00:00" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"/>

                    <TextBlock Grid.Column="0" Grid.Row="3" Text="Uplink" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="1" Grid.Row="3" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="1" Grid.Row="3" Text="2400MHz" VerticalAlignment="Center" Margin="10,0,15,0" TextTrimming="CharacterEllipsis"/>
                    <Button  Grid.Column="1" Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,2,32,0" Style="{DynamicResource TipButton}">
                        <Rectangle Style="{DynamicResource Icon}" ToolTip="{loc:Localization sEdit}" Width="7" Height="7">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource IconEdit}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button>
                    

                    <TextBlock Grid.Column="2" Grid.Row="3" Text="Downlink" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    <Rectangle  Grid.Column="3" Grid.Row="3" Style="{StaticResource Splitter}"/>
                    <TextBlock  Grid.Column="3" Grid.Row="3" Text="2400MHz" VerticalAlignment="Center" Margin="10,0,15,0" TextTrimming="CharacterEllipsis"/>
                    <Button  Grid.Column="3" Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,2,32,0" Style="{DynamicResource TipButton}">
                        <Rectangle Style="{DynamicResource Icon}" ToolTip="{loc:Localization sEdit}" Width="7" Height="7">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource IconEdit}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button>



                    <TextBlock Text="AOS" Margin="10,30,0,0"/>
                </Grid>
            </GroupBox>

            <ListView Grid.Row="1" VerticalAlignment="Stretch">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="20">
                            <GridViewColumnHeader IsEnabled="False"/>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="40" Header="No">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Width="120" Header="Name">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Width="90" Header="Relative Time">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        
                        <GridViewColumn Width="70" Header="Status">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DataTemplate>
</ResourceDictionary>