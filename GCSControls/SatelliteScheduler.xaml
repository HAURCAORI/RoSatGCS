<UserControl x:Class="GCSControls.SatelliteScheduler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:GCSControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="3" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="2" Background="Transparent">
            <b:Interaction.Behaviors>
                <local:TimeBarBehavior TimeLine="{Binding TimeLine, Mode=TwoWay}"/>
            </b:Interaction.Behaviors>
            <local:TimeBar x:Name="xTimeBar" TimeLine="{Binding TimeLine, Mode=TwoWay}"/>
        </Grid>
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ResizeDirection="Columns"/>
        <Grid Grid.Row="1" Grid.Column="2" Background="Transparent">
            <b:Interaction.Behaviors>
                <local:TimeBarBehavior TimeLine="{Binding TimeLine, Mode=TwoWay}"/>
            </b:Interaction.Behaviors>
            <local:TimeSlots x:Name="xTimeSlots" TimeLine="{Binding TimeLine, Mode=TwoWay}" TimeSlotItems="{Binding TimeSlotItems}"/>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="0" Background="Transparent">
            <ListBox x:Name="xListBox" ItemsSource="{Binding TimeSlotItems}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     UseLayoutRounding="True" SnapsToDevicePixels="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="20">
                            <TextBlock Text="{Binding Name}" Margin="5,0" TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Name}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <Border BorderBrush="#FFEEEEEE" BorderThickness="0.5" Height="20">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
